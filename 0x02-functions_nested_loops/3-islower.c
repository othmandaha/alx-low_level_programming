#include "main.h"

/**
 * _islower - checking if the character is lowercase or uppercase
 *
 * @c: a character for checking wether it is lowecase or uppercase
 *
 * Return: If char is lower return = 1. If char is upper return = 0.
 */

int _islower(int c)
{
	int val;

	if (c < 91)
	{
		v = 0;
	}
	else
	{
		v = 1;
	}

	return (v);
}
