#include "main.h"

/**
 * _abs - absolute value of integers
 *
 * @n: the integer being checked
 *
 * Return: return absolute value of n
 */

int _abs(int n)
{
	int v;

	if (n > 0)
	{
		n = n;
	}
	else if
	{
		n = n + n;
	}
	return (n);
}
