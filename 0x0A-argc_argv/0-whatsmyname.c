#include <stdio.h>

/**
 * main - the starting point of the program
 * @argc: argument count
 * @argv: arguments
 *
 * Return: always 0 means success.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
